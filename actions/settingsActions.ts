"use server";

import { auth, db } from "@/lib/auth/auth";
import { settingsSchema } from "@/lib/validation/settingSchema";

export function getSettings(formData: FormData) {
  // TODO
}

export function updateSettings(formData: FormData) {
  // TODO
}
